---
import { getCollection } from "astro:content";

const posts = (await getCollection("insights"))
  .sort((a,b) => b.data.date - a.data.date);

function readingTime(text){
  const words = text.split(/\s+/).length;
  return Math.ceil(words / 200);
}
---

<html>
<head>

<title>Perspectives — Patrik Hallén</title>

<style>

:root{
--accent:#c8102e;
}

body{
font-family: system-ui, -apple-system, sans-serif;
max-width: 900px;
margin:auto;
padding:60px 20px;
line-height:1.65;
color:#111;
}

header{
margin-bottom:70px;
}

h1{
font-size:48px;
margin:0;
}

.subtitle{
color:#666;
margin-top:10px;
}

.author{
color:#777;
margin-top:20px;
}

.article{
margin-bottom:80px;
}

.article img{
width:100%;
border-radius:12px;
margin-bottom:20px;
aspect-ratio:16/9;
object-fit:cover;
}

.article h2{
font-size:28px;
margin:0 0 10px 0;
}

.meta{
font-size:14px;
color:#777;
margin-bottom:12px;
}

.summary{
color:#444;
}

a{
text-decoration:none;
color:inherit;
display:block;
}

a:hover h2{
color:var(--accent);
}

.tag{
display:inline-block;
font-size:12px;
letter-spacing:.05em;
text-transform:uppercase;
color:#666;
margin-right:10px;
}

/* Featured article */

.featured{
border-left:6px solid var(--accent);
padding-left:24px;
margin-bottom:100px;
}

.featured h2{
font-size:36px;
}

</style>

</head>

<body>

<header>

<h1>Perspectives</h1>

<p class="subtitle">
Ideas on structure, transformation and execution.
</p>

<p class="author">
Patrik Hallén  
<br>
Member of Andersen Consulting
</p>

</header>

{posts.map((post, index) => {

const minutes = readingTime(post.body);

return (

<article class={index === 0 ? "article featured" : "article"}>

<a href={`/insights/${post.id}`}>

<img src={`/heroes/${post.id}.png`} alt="hero" />

<div class="meta">

<span class="tag">{post.data.topic}</span>

<span>
{new Date(post.data.date).toLocaleDateString("en-GB")}
</span>

<span> • {minutes} min read</span>

</div>

<h2>{post.data.title}</h2>

<p class="summary">
{post.data.summary}
</p>

</a>

</article>

)

})}

</body>

</html>
